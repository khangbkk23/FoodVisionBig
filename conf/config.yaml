# conf/config.yaml
dataset:
  name: "food101"
  data_dir: "data/"
  image_size: 224
  batch_size: 32
  num_workers: 4
  pin_memory: true
  download: true

training:
  epochs: 50
  learning_rate: 0.001
  weight_decay: 1e-4
  seed: 42